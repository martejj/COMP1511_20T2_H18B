

int instruction = 0;

int success = scanf("%d", &instruction);

if (success == 1) {
    if (instruction == 1) {
        // Scan in 1 more and square it.
    } else if (instruction == 2) {
        // scan in 2 more and power them.
    }
}
