#include <stdio.h>

int is_even(int number);

int main(void) {
    
    printf("%d\n", 1);
    
}

int is_even(int number) {
    
    if (number%2 == 0) {
        return 1;
    } else {
        return 0;
    }
}
